import 'package:flutter/material.dart';

class GroupRequestSendView extends StatelessWidget {
  const GroupRequestSendView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Group request view"),
    );
  }
}
